<!DOCTYPE html>
<lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VVS Blog post</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
     integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
     <link rel="stylesheet" href="/styles/styles.css">
</head>
<body>
    <div class="container">
    <%- include("partials/header.ejs") %>
   <div class="author">
    <h1 class="author_into">Hi, I am Akhil</h1>
    <p class="author_body">I do web development and also a student</p>
   </div>
   
   <img src="/images/keyboard.png" alt="Tying on a keyboard" 
    class="keyboard" width="981" height="528">
  <div class="create">
   <a href="/new" class="btn btn-lg" style="background-color: #1C1C1E; color: white; border: none;">Create new post</a>
  </div>  
  <main class="main"> 

    <section class="blogs">
        <h2 class="latest">Latest posts</h2>
        <!-- Displaying the fixed post -->
      <div class="card mt-4">
        <div class="card-body">
           <h4 class="card-title"> <%= fixedPost.title %> </h4>
          <div class="card-subtitle text-muted mb-2">
           <%= fixedPost.createdAt.toLocaleDateString() %>
          </div>
          <div class="card-text mb-2"><%= fixedPost.content %></div>
        </div>
      </div>
        
        <!--Displaying dynamic posts-->
      <% posts.forEach(post =>{ %>
          <div class="card mt-4">
            <div class="card-body">
              <h4 class="card-title"> <%= post.title %></h4>
              <div class="card-subtitle text-muted mb-2">
                <%= post.createdAT.toLocaleDateString() %>
              </div>
              <div class="card-text mb-2"><%= post.content %></div>
              <div class=" mt-3 mb-3">
                <a href="/edit/<%= post.id %>" class="btn" style="background-color: #1C1C1E; color: white; border: none;">Edit</a>
                <form action="/delete/<%= post.id %>" method="POST" style="display: inline;">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit" class="btn btn-danger" >Delete</button>
                </form>
            </div>
            </div>
          </div>
          <%  }) %>
    </section>

  </main>
    <%- include("partials/footer.ejs") %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</div>
</body>
</html>
